# V1 Progress Tracker

Status: M3TC3E complete; tracker active

## Purpose

This document is the single progress ledger for the V1 rewrite. It records the
current milestone status, the issue/PR label vocabulary, and the update protocol
for milestone, epic, slice, and test-track work.

The architecture and implementation-plan documents remain the source of truth
for scope and decisions. This tracker only records execution state.

## Tracking Rules

1. Track work with roadmap labels in issues, PR titles, branch names, and commit
   messages only.
2. Do not use roadmap labels in production crate names, module names, file names,
   type names, function names, test names, feature flags, error codes, metrics,
   CLI commands, config keys, or user-facing text.
3. Every implementation PR should name exactly one implementation slice or one
   milestone-level planning task.
4. Every test PR should name exactly one test slice or one milestone-level test
   task.
5. A milestone closes only after both its implementation track and test track
   close.
6. If a slice grows beyond one focused pass, split the slice before adding
   temporary compatibility code.
7. Update this tracker when a milestone, epic, or slice changes status.

## Status Vocabulary

| Status | Meaning |
|---|---|
| `Complete` | Scope is implemented, reviewed, fixed, and verified for the slice or milestone gate. |
| `Ready` | Inputs are present and the work may start. |
| `Planned` | Scope exists but an upstream milestone or decision still gates execution. |
| `In Progress` | Active implementation or review is underway. |
| `Review` | Implementation is complete and waiting on review or review response. |
| `Fix` | Review found issues and fixes are underway. |
| `Blocked` | Work cannot proceed until a named blocker closes. |
| `Deferred` | Intentionally outside the current V1 execution path. |

## Label Vocabulary

Use these labels for issues and PRs in any external tracker.

| Label kind | Shape | Example |
|---|---|---|
| V1 effort | `v1` | `v1` |
| Track | `track:implementation`, `track:test`, `track:docs` | `track:test` |
| Milestone | `milestone:M{n}` | `milestone:M3` |
| Epic | `epic:M{n}{letter}` or `epic:M{n}T{letter}` | `epic:M6E`, `epic:M6TD` |
| Slice | `slice:M{n}{letter}{number}` or `slice:M{n}T{letter}{number}` | `slice:M3C1`, `slice:M3TA1` |
| Status | `status:planned`, `status:ready`, `status:in-progress`, `status:review`, `status:fix`, `status:complete`, `status:blocked`, `status:deferred` | `status:review` |
| Risk | `risk:format`, `risk:recovery`, `risk:boundary`, `risk:performance`, `risk:security` | `risk:recovery` |

PR and issue titles should start with the slice or epic code followed by a short
domain title, for example:

```text
M3C1: Add storage row golden vectors
M6TD2: Cover recipe freshness degradation
```

## Current Milestone Status

| Milestone | Title | Implementation status | Test status | Gate status | Next action |
|---|---|---|---|---|---|
| `M0` | Architecture freeze and tracking | Complete | Complete | Complete | M0 closed. |
| `M1` | Core-next | Complete | Complete | Complete | M1 closed; start `M2A` and `M2TA` when storage-next implementation begins. |
| `M2` | Storage-next testkit and crate skeleton | Complete | Complete | Complete | M2 closed. |
| `M3` | Storage-next backend, layout, format, and durable services | In Progress | In Progress | In Progress | `M3TC3E` closes the snapshot/checkpoint/sidecar fault-window matrix. Continue with `M3E4` quarantine service mechanics and recovery integration. |
| `M4` | Storage-next table, branch, commit, recovery, and L9 API | Planned | Planned | Planned | Start after durable bytes and services are stable. |
| `M5` | Engine-next persistence adapter and control plane | Planned | Planned | Planned | Start after L9 is consumable. |
| `M6` | Engine-next product semantics | Planned | Planned | Planned | Start after engine persistence/control plane are stable. |
| `M7` | Inference-next hardening | Planned | Planned | Planned | May start now if inference-next should run parallel with storage path. |
| `M8` | Intelligence-next orchestration | Planned | Planned | Planned | Start after engine surfaces and inference task contracts are ready. |
| `M9` | Executor, CLI, SDK, tests, benches, and docs cutover | Planned | Planned | Planned | Start after product surfaces stabilize. |
| `M10` | V1 readiness hardening | Planned | Planned | Planned | Start after cutover. |

## Current Epic Status

| Epic | Title | Track | Status | Blocks | Next action |
|---|---|---|---|---|---|
| `M0D` | Tracking setup | Implementation | Complete | none | M0 can close and M1 can start. |
| `M1A` | Core-next crate skeleton | Implementation | Complete | none | `M1A1` complete; continue M1 atom work. |
| `M1TA` | Core atom unit tests | Test | Complete | none | Atom tests cover construction, validation, ordering, parsing, display, and serde behavior through `M1C`. |
| `M1B` | Core atoms | Implementation | Complete | none | `M1B1` through `M1B4` complete. |
| `M1C` | Core atom encodings | Implementation | Complete | none | Parse, display, and serde behavior complete. |
| `M1TB` | Core atom property tests | Test | Complete | none | Generated ordering, boundary, invalid-input, and parse/display/serde round-trip coverage complete. |
| `M1TC` | Core-next dependency guard | Test | Complete | none | Guard fails if core-next declares or resolves a dependency on any other Strata crate. |
| `M1D` | Core-next boundary documentation | Implementation | Complete | none | Implemented public exports and rejected candidates are documented. |
| `M1TD` | Core-next API audit | Test | Complete | none | Public API snapshot guard is checked in and runs with the core-next test suite. |
| `M2A` | Storage-next crate skeleton | Implementation | Complete | none | `M2A1` complete. |
| `M2B` | Backend contract shell | Implementation | Complete | none | `M2B1` complete. |
| `M2C` | Memory and local backend shells | Implementation | Complete | none | `M2C1` complete. |
| `M2TA` | Backend conformance smoke | Test | Complete | none | Private backend conformance smoke covers memory and local filesystem shells. |
| `M2D` | Testkit foundation | Implementation | Complete | none | `M2D1` complete; boundary guards closed under `M2TD`. |
| `M2E` | Harness scaffolding | Implementation | Complete | none | Harness entry points and invocation notes are in place. |
| `M2TB` | Feature matrix | Test | Complete | none | `cargo-hack` feature powerset gate passes for storage-next. |
| `M2TC` | WASM cache compile | Test | Complete | none | wasm32 memory/cache compile gate passes with localfs excluded. |
| `M2TD` | Testkit boundary guards | Test | Complete | none | Compile probes prove testkit and fault APIs stay feature-gated. |
| `M3A` | Backend operations | Implementation | Complete | none | `M3A1` complete; backend mode capability validation is in place. |
| `M3B` | Object layout | Implementation | Complete | none | `M3B1` and `M3B2` complete; continue with format codec work. |
| `M3C` | Format codec | Implementation | Complete | none | `M3C1` through `M3C5` complete; durable service work now continues in `M3E`. |
| `M3D` | Durable publisher | Implementation | Complete | none | Durable publisher implementation and publish fault-window tests are complete. |
| `M3E` | Durable services | Implementation | In Progress | none | `M3E1` adds manifest services, `M3E2` adds WAL service mechanics, and `M3E3A-D` add snapshot/checkpoint/optional-sidecar mechanics. Continue with quarantine service mechanics. |
| `M3TA` | Format golden tests | Test | In Progress | none | `M3C1` adds internal-key and storage-row goldens; `M3C2` adds manifest, watermark, and segment metadata goldens; `M3C3` adds WAL segment, envelope, and record goldens; `M3C4` adds snapshot header, section, and container goldens. Remaining service/object-family goldens land with M3E or M4 slices when those durable objects exist. |
| `M3TB` | Format fuzz tests | Test | In Progress | none | `M3C5` adds the first cargo-fuzz package and initial parser targets for manifest, snapshot envelope, storage row, and WAL record bytes. `M3TB2` remains optional after `M3TC2`; decide before closing the M3 durable-service test gate. |
| `M3TC` | Durable fault-window tests | Test | In Progress | none | `M3TC1` covers durable-publish fault windows; `M3TC2A-G` close the WAL durable-service matrix; `M3TC3A-E` close the snapshot, checkpoint, and sidecar durable-service matrix. Continue with quarantine fault-window tests after `M3E4` lands. |
| `M3TE` | Manifest service tests | Test | Complete | none | `M3TE1A` through `M3TE1E` closed; manifest services have reference-grade load, update, publish-failure, opacity, and error-precision coverage. |
| `M3TF` | Backend conformance | Test | In Progress | none | `M3A1` extended private backend conformance with storage-mode validation; full M3TF closes after durable services exist. |
| `M7A` | Inference task traits | Implementation | Planned | none | May start now if parallel inference work is scheduled. |

## M0 Closure Record

M0 is closed by the following artifacts and recorded verification gates:

| Code | Status | Artifact |
|---|---|---|
| `M0TA` | Complete | Transient verification gate; see the M0TA link check record below. |
| `M0TB` | Complete | Transient verification gate; see the M0TB terminology scan record below. |
| `M0TC` | Complete | `docs/architecture/v1-boundary-baseline.md` |
| `M0A` | Complete | `docs/architecture/v1-document-inventory.md` |
| `M0B` | Complete | `docs/architecture/v1-open-question-register.md` |
| `M0C` | Complete | Roadmap and target crate-shape standards alignment. |
| `M0TD` | Complete | `docs/architecture/v1-engineering-standards-baseline.md` |
| `M0TE` | Complete | `docs/architecture/v1-test-inventory.md` |
| `M0D` | Complete | `docs/architecture/v1-progress-tracker.md` |

## M0TA Link Check Record

`M0TA` is a transient verification gate with no separate artifact file. Its
scope was active V1 architecture, product, and spec documents outside archive
directories.

Closure required local markdown links and backticked local document paths to
resolve, except for classified future references to not-yet-created `*-next`
crates and explicit negative examples. During M0, no real broken required
document links were found.

## M0TB Terminology Scan Record

`M0TB` is a transient verification gate with no separate artifact file. Its
scope was active V1 documents, target crate-shape documents, and implementation
plans.

Closure required cleanup-era milestone vocabulary to be removed, moved to
historical context, or classified as intentional evidence. During M0, the only
domain anti-pattern hits in target crate-shape documents were explicit negative
examples that say not to create production types or modules named `Helper`.

## M1 Closure Record

M1 is closed by the following artifacts and verification gates:

| Code | Status | Artifact |
|---|---|---|
| `M1A` | Complete | `crates/core-next` crate skeleton and workspace membership. |
| `M1TA` | Complete | `crates/core-next/tests/atom_contract.rs` plus module-local atom tests. |
| `M1B` | Complete | `crates/core-next/src/branch.rs`, `crates/core-next/src/version.rs`, and `crates/core-next/src/time.rs`. |
| `M1C` | Complete | Core atom parse, display, serde, and durable representation tests. |
| `M1TB` | Complete | `crates/core-next/tests/atom_properties.rs`. |
| `M1TC` | Complete | `crates/core-next/tests/dependency_guard.rs`. |
| `M1D` | Complete | `docs/architecture/core-next-architecture.md` Implemented M1 Boundary section. |
| `M1TD` | Complete | `crates/core-next/tests/public_api_snapshot.rs` and `crates/core-next/tests/snapshots/public_api.txt`. |

## M2 Closure Record

M2 is closed by the following artifacts and verification gates:

| Code | Status | Artifact |
|---|---|---|
| `M2A` | Complete | `crates/storage-next` crate skeleton, workspace membership, crate-level unsafe policy, feature declarations, and initial module tree. |
| `M2B` | Complete | Backend capability vocabulary, storage-mode requirements, memory backend capability facts, object name/prefix validation, backend metadata/fence/range types, backend error categories, and minimal synchronous backend trait. |
| `M2C` | Complete | Memory backend implementation and local filesystem backend shell with basic object operations, capability declarations, range reads, metadata, deterministic list/delete behavior, local object-file encoding for prefix-collision safety, symlink rejection on localfs paths, and current-code cross-check against existing cache/local filesystem behavior. |
| `M2TA` | Complete | Private backend conformance smoke in `crates/storage-next/src/backend/conformance.rs`, covering claimed basic object capabilities, missing-object behavior, range boundaries, overwrite semantics, prefix listing, delete behavior, and unsupported conditional operations for memory and local filesystem backends. |
| `M2D` | Complete | Crate-private `test_support`, feature-gated doc-hidden `testkit`, backend selector parsing, deterministic faulting backend wrapper with a public operation gate, fault script/rule/call types, and coverage across default and no-default testkit/fault-injection builds. |
| `M2TD` | Complete | `crates/storage-next/tests/testkit_boundary.rs`, with external compile probes proving `testkit` is unavailable without its feature, selector APIs are available with `testkit`, fault APIs require `fault-injection`, and source guards keep testkit exports doc-hidden. |
| `M2E` | Complete | Storage harness scaffolding under `crates/storage-next/tests/`, `tests/common/mod.rs`, `tests/README.md`, `testdata/goldens/storage-format-v1/README.md`, and `fuzz/`, with smoke entry points for conformance, properties, goldens, fault windows, crash recovery, stress, and API harnesses without durable-behavior claims. |
| `M2TB` | Complete | `cargo hack -p strata-storage-next --feature-powerset --depth 2 --locked check --all-targets` covers no-default, default, each feature, and pairwise combinations. `cargo test -p strata-storage-next --no-default-features --features testkit --test backend_conformance --locked` proves unavailable backend selections report a targeted `localfs` feature requirement. |
| `M2TC` | Complete | `cargo check -p strata-storage-next --no-default-features --target wasm32-unknown-unknown --all-targets --locked` proves the memory/cache build compiles on wasm with `localfs` excluded. `cargo test -p strata-storage-next --test testkit_boundary --locked localfs_feature_is_rejected_for_wasm_builds` proves default-feature wasm builds fail with a targeted `localfs` unsupported message. |

## M3 Execution Record

M3 is active. Closed work is recorded here until the full milestone gate can
move to Complete.

| Code | Status | Artifact |
|---|---|---|
| `M3A1` | Complete | `crates/storage-next/src/config/mode.rs` adds cache, durable-local standard, durable-local always, and object-durable-candidate backend capability validation. `crates/storage-next/src/backend/conformance.rs` proves current memory and local filesystem basic object backends accept cache mode and reject durable modes until durable publish, sync, writer-lock, and object-fencing capabilities are implemented. `docs/architecture/implementation-plans/m3-porting-log.md` records the source-map and retirement disposition. |
| `M3B1` | Complete | `crates/storage-next/src/layout/mod.rs` adds reserved object families and constructors for manifest, WAL segment, table, snapshot, temporary, quarantine, lock, and metadata object names and prefixes. Layout tests prove fixed-width ordered IDs, prefix ownership, component validation, quarantine separation, and follower-state path exclusion. `docs/architecture/implementation-plans/m3-porting-log.md` records the source-map and retirement disposition. |
| `M3B2` | Complete | `crates/storage-next/src/layout/tests.rs` adds property-style tests for WAL/snapshot ordering, table/temporary/quarantine prefix ownership, and invalid component rejection while keeping the production layout module under the V1 file-size threshold. `crates/storage-next/tests/object_layout_properties.rs` adds a source guard proving reserved durable layout names stay owned by layout/object/local-fs boundary code. `docs/architecture/implementation-plans/m3-porting-log.md` records the source-map and retirement disposition. |
| `M3C1` | Complete | `crates/storage-next/src/row/mod.rs` adds storage-space id, physical key, internal key, and storage row records. `crates/storage-next/src/format/key.rs` and `crates/storage-next/src/format/storage_row.rs` add strict V1 internal-key and storage-row encoders/decoders. `crates/storage-next/testdata/goldens/storage-format-v1/` adds golden vectors for ordinary internal keys, zero-byte escaped internal keys, put rows, and tombstone rows. `docs/spec/strata-storage-format-v1.md` records the frozen row format and internal-key ordering exception. |
| `M3C2` | Complete | `crates/storage-next/src/format/manifest.rs`, `crates/storage-next/src/format/watermark.rs`, and `crates/storage-next/src/format/segment_metadata.rs` add strict V1 encoders/decoders for database manifest bytes, snapshot watermark bytes, and WAL segment metadata sidecars, including invalid recovery-fact rejection. `crates/storage-next/testdata/goldens/storage-format-v1/` adds identity-codec manifest, empty/present watermark, and segment metadata golden vectors. `docs/spec/strata-storage-format-v1.md` records the exact V1 manifest, watermark, and sidecar byte formats. |
| `M3C3` | Complete | `crates/storage-next/src/format/wal.rs` adds strict V1 encoders/decoders for WAL segment headers, codec-aware outer record envelopes, and inner WAL records. `crates/storage-next/testdata/goldens/storage-format-v1/` adds segment header, empty-payload record, non-empty-payload record, and envelope golden vectors. `docs/spec/strata-storage-format-v1.md` records the exact V1 WAL header, envelope, and record byte formats. |
| `M3C4` | Complete | `crates/storage-next/src/format/snapshot.rs` adds strict V1 encoders/decoders for snapshot headers, section envelopes, and whole containers with footer CRC validation. `crates/storage-next/testdata/goldens/storage-format-v1/` adds identity-codec header, empty-section envelope, and single-section container golden vectors. `docs/spec/strata-storage-format-v1.md` records the exact V1 snapshot container and section envelope byte formats. |
| `M3C5` | Complete | `crates/storage-next/fuzz/` is now a cargo-fuzz package with initial targets for manifest, snapshot envelope, storage row, and WAL record decoder families. `crates/storage-next/src/testkit/format_fuzz.rs` exposes a hidden testkit routing surface for fuzz targets without making format internals production API. |
| `M3D1` | Complete | `crates/storage-next/src/backend/publish.rs` adds the backend publish contract and classified publish-failure windows. `crates/storage-next/src/backend/local_fs.rs` implements Unix durable publication with unique temporary files, temp file sync, atomic no-clobber create, atomic replace, and parent-directory sync; non-Unix local filesystem builds do not advertise durable publish/sync yet. `crates/storage-next/src/backend/memory.rs` implements explicit non-durable cache publication. `crates/storage-next/src/service/publish.rs` exposes the thin L4 publisher wrapper over layout-owned object names. `crates/storage-next/src/testkit/mod.rs` can now observe and fault publish operations. |
| `M3TC1` | Complete | `crates/storage-next/src/backend/local_fs.rs` adds backend-local Unix fault hooks and tests for temporary creation, temporary write, temporary sync, final publish, and parent-directory sync. The tests assert classified publish errors, final object visibility, and generated temporary object cleanup without exposing the hooks through the public testkit. |
| `M3E1` | Complete | `crates/storage-next/src/service/manifest.rs` adds database manifest service mechanics over V1 manifest bytes and payload-opaque table manifest publication over layout-owned table manifest object names. The service distinguishes missing, corrupt, codec-mismatch, invalid-recovery-fact, and publish-window failures without importing branch/table semantics. `crates/storage-next/src/format/manifest.rs` now rejects present-but-zero snapshot recovery facts before bytes can be persisted. `docs/architecture/implementation-plans/m3e1-manifest-test-suite-plan.md` defines the follow-up `M3TE1` reference-grade manifest test suite. |
| `M3E2` | Complete | `crates/storage-next/src/backend/mod.rs` adds object-name based append/sync backend primitives and `crates/storage-next/src/backend/local_fs.rs` implements them for local durable WAL segments. `crates/storage-next/src/service/wal.rs` adds the durable-local WAL service: segment create/open, append, standard/always durability policy, rotation, strict reads, partial-tail facts, and covered-segment deletion. `crates/storage-next/src/service/wal/tests.rs` covers cache backend rejection, header validation, append/read roundtrip, policy sync behavior, rotation, corruption, partial tails, and active segment protection. `docs/architecture/implementation-plans/m3e2-wal-test-suite-plan.md` defines the follow-up `M3TC2` reference-grade WAL test suite. |
| `M3E3A` | Complete | `crates/storage-next/src/service/snapshot.rs` adds snapshot publish/load basics over V1 snapshot container bytes: create-only durable publish, optional and required load, codec and database identity validation, zero snapshot-fact rejection before backend access and after decode, header/object id mismatch rejection, corrupt-byte fail-closed decode routing, publish-failure kind propagation, and `visit_sections` for borrowed section inspection without materializing payloads. `crates/storage-next/src/format/mod.rs` exposes snapshot container and borrowed visitor format functions to L4, and `crates/storage-next/src/service/mod.rs` wires the service for later lifecycle/recovery consumers. `docs/architecture/implementation-plans/m3-porting-log.md` records the source-map and retirement disposition. |
| `M3E3B` | Complete | `crates/storage-next/src/service/snapshot/listing.rs` adds snapshot listing over `ObjectLayout::snapshot_prefix()`, exact lowercase `snapshots/<16-hex-id>` parsing, latest-snapshot selection by highest listed id, and caller-driven pruning with live-snapshot and newest-retained protection. `crates/storage-next/src/service/snapshot/listing_tests.rs` covers numeric ordering, malformed snapshot-family fail-closed behavior, weak-prefix family ignores, list failure routing, malformed prune listings before any delete, live/newest protection, retain count clamping, delete-failure reports, and delete-capability preflight. `crates/storage-next/src/service/snapshot.rs` and `crates/storage-next/src/service/mod.rs` wire the snapshot object/delete report facts for later lifecycle/recovery consumers. |
| `M3E3C` | Complete | `crates/storage-next/src/service/checkpoint.rs` adds mechanical checkpoint sequencing over database MANIFEST and snapshot services. It validates existing MANIFEST identity, reuses that loaded MANIFEST for active WAL persistence, publishes immutable snapshot bytes, persists snapshot id plus watermark only after snapshot publish success, returns typed orphan snapshot facts for final MANIFEST no-visible failures, and returns typed final-MANIFEST uncertainty for `VisibilityUnknown` / `VisibleDurabilityUnconfirmed`. `crates/storage-next/src/service/checkpoint/tests.rs` and `tests/support.rs` cover success order, single-load active-WAL update, invalid input facts before mutation, missing/corrupt/mismatched MANIFEST failures before snapshot publish, active-WAL MANIFEST failure, every snapshot publish `PublishFailureKind`, final-update orphan facts, final-MANIFEST uncertainty, direct snapshot loadability, and previous MANIFEST fact preservation. Verification: `cargo test -p strata-storage-next --locked service::checkpoint`, `cargo test -p strata-storage-next --locked`, `cargo test -p strata-storage-next --no-default-features --locked`, `cargo test -p strata-storage-next --all-features --locked`, `cargo test -p strata-storage-next --features testkit,fault-injection --locked`, `cargo test -p strata-storage-next --no-default-features --features testkit,fault-injection --locked`, `cargo clippy -p strata-storage-next --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage-next --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo doc -p strata-storage-next --no-deps --locked`, and `cargo fmt --package strata-storage-next --check`. Engineering scan: files stay below review thresholds after splitting checkpoint support; no forbidden naming hits except `from_slice` false positive. |
| `M3E3D` | Complete | `crates/storage-next/src/layout/mod.rs` adds `meta/wal/<16-hex-segment-id>` object naming for optional WAL segment metadata sidecars. `crates/storage-next/src/service/sidecar.rs` adds `WalSegmentMetadataSidecarService` over the M3C2 segment metadata codec: durable replace publish, load as present/missing/corrupt fallback facts, typed backend read failures, publish failure kind preservation, and non-fatal delete reports. Missing or corrupt sidecars do not affect authoritative WAL or snapshot facts. `crates/storage-next/src/service/sidecar/tests.rs` covers zero-id rejection across load/publish/delete, memory-backend durable rejection, and local filesystem durable publication. `docs/architecture/storage-next/l2-object-layout.md` records the implemented sidecar object namespace, and `docs/architecture/implementation-plans/m3-porting-log.md` records the source-map and retirement disposition. Verification: `cargo test -p strata-storage-next --locked service::sidecar`, `cargo test -p strata-storage-next --locked wal_segment_metadata`, `cargo test -p strata-storage-next --locked`, `cargo test -p strata-storage-next --no-default-features --locked`, `cargo test -p strata-storage-next --all-features --locked`, `cargo test -p strata-storage-next --features testkit,fault-injection --locked`, `cargo test -p strata-storage-next --no-default-features --features testkit,fault-injection --locked`, `cargo clippy -p strata-storage-next --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage-next --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo doc -p strata-storage-next --no-deps --locked`, and `cargo fmt --package strata-storage-next --check`. |
| `M3TC2A` | Complete | `crates/storage-next/src/service/wal/tests.rs` adds WAL construction, capability, and listing hardening: exact segment-id and segment-size rejection, exact per-capability open rejection across all seven WAL-required backend capabilities, exact memory-backend unsupported capability routing, missing-segment header creation, existing-segment no-rewrite behavior, malformed header and wrong-database routing, invalid listed WAL object matrix including zero segment id as a list-context invalid object, backend-order-independent segment reads with accepted gaps, adjacent-prefix filtering, and max-segment rotation overflow. The initial full-suite run caught a reserved layout string in the test itself; the test now derives the WAL prefix through `ObjectLayout`. Sensitivity probes: temporarily removed `AppendObject` from `WAL_REQUIRED_CAPABILITIES`, returned `WalServiceError::List` for invalid listed WAL components, and removed segment sorting so listed segments followed backend order; the targeted tests failed under each mutation and passed after revert. Narrow verification: `cargo test -p strata-storage-next --locked service::wal` and `cargo test -p strata-storage-next --no-default-features --locked service::wal`. Broad verification: `cargo test -p strata-storage-next --locked`, `cargo test -p strata-storage-next --no-default-features --locked`, `cargo clippy -p strata-storage-next --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage-next --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo fmt --package strata-storage-next --check`, and `git diff --check -- crates/storage-next/src/service/wal/tests.rs docs/architecture/v1-progress-tracker.md docs/architecture/implementation-plans/m3tc2-wal-test-implementation-plan.md docs/architecture/implementation-plans/m3e2-wal-test-suite-plan.md`. Engineering scan: no forbidden naming hits; roadmap-label scan hits only `from_slice` false positives. |
| `M3TC2B` | Complete | `crates/storage-next/src/service/wal/tests/append.rs` adds WAL append and rotation hardening: exact-fill records stay in the current segment, one-byte-over records rotate before append, oversized records fail before backend mutation, active metadata mismatch is rejected before append both before and after rotation, backend append failure and wrong append reports leave service state clean, and a proptest append model checks 1 to 128 records across 1 KiB to 8 KiB segment sizes against segment ids, offsets, dirty facts, active metadata, reopened reads, and object-size bounds. `crates/storage-next/src/service/wal/tests/localfs.rs` holds localfs-only WAL behavior after the split from the root test module, keeping each unit-test file below the engineering-standard split threshold. Sensitivity probes: temporarily changed rotation from `>` to `>=`, bypassed record-too-large preflight, and disabled active metadata tracking; targeted tests or the append model failed under each mutation and passed after revert. Narrow verification: `PROPTEST_CASES=2048 cargo test -p strata-storage-next --locked wal_append_model`, `cargo test -p strata-storage-next --locked service::wal`, and `cargo test -p strata-storage-next --no-default-features --locked service::wal`. Broad verification: `cargo test -p strata-storage-next --locked`, `cargo test -p strata-storage-next --no-default-features --locked`, `cargo test -p strata-storage-next --all-features --locked`, `cargo clippy -p strata-storage-next --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage-next --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo doc -p strata-storage-next --no-deps --locked`, `cargo fmt --package strata-storage-next --check`, and `git diff --check -- crates/storage-next/src/service/wal.rs crates/storage-next/src/service/wal/tests.rs crates/storage-next/src/service/wal/tests/append.rs crates/storage-next/src/service/wal/tests/localfs.rs`. Engineering scan: no forbidden naming hits; roadmap-label scan hits only Rust `slice` API false positives. |
| `M3TC2C` | Complete | `crates/storage-next/src/service/wal/tests/durability.rs` adds WAL durability-policy and sync-failure hardening over a sync-fault backend. `standard` tests prove append does not force sync, dirty bytes and records are tracked, `force_durable` and `close` sync the active segment and clear counters on success, and both force/close sync failures return typed sync errors while preserving dirty facts. `always` tests prove append syncs before success, successful appends return `forced_durable = true` with clean counters, and failed sync after a successful backend append leaves the record visible with durability unconfirmed: active segment id and object size advanced, active metadata updated, dirty counters non-zero, `read_all` returns the record without truncation, and a later successful append resumes at the visible offset. Sync error routing preserves `NotFound`, `Interrupted`, and `Unavailable` as backend sync failures. Sensitivity probes: temporarily cleared dirty counters on `force_durable` failure and temporarily removed active metadata tracking before sync; targeted durability tests failed under each mutation and passed after revert. Narrow verification: `cargo test -p strata-storage-next --locked service::wal::tests::durability` and `cargo test -p strata-storage-next --locked service::wal`. Broad verification: `cargo test -p strata-storage-next --locked`, `cargo test -p strata-storage-next --no-default-features --locked`, `cargo test -p strata-storage-next --all-features --locked`, `cargo clippy -p strata-storage-next --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage-next --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo doc -p strata-storage-next --no-deps --locked`, `cargo fmt --package strata-storage-next --check`, and `git diff --check -- crates/storage-next/src/service/wal.rs crates/storage-next/src/service/wal/tests.rs crates/storage-next/src/service/wal/tests/durability.rs docs/architecture/v1-progress-tracker.md docs/architecture/implementation-plans/m3tc2-wal-test-implementation-plan.md docs/architecture/implementation-plans/m3e2-wal-test-suite-plan.md`. |
| `M3TC2D` | Complete | `crates/storage-next/src/service/wal/tests/corruption.rs` adds the WAL corruption matrix with named byte-boundary mutations and exact inner `FormatError` assertions for the load-bearing source kind. Coverage includes segment header magic/version/checksum/segment-id/database-id cases, envelope length and checksum corruption, final-envelope truncation, non-latest truncation, encoded-record byte mutation, inner record length and length-CRC corruption, payload CRC and payload-byte corruption, branch-id byte and truncation corruption, timestamp truncation, and latest versus non-latest trailing garbage. The tests keep the header-id asymmetry explicit: segment-id mismatch is `Format`, while a valid header checksum for another database id is `DatabaseMismatch`. `crates/storage-next/src/service/wal/tests/support.rs` now owns shared WAL test builders and fake backends so WAL test files stay below the engineering-standard review threshold. Sensitivity probes: temporarily treated all partial tails as recoverable, temporarily mapped database-id mismatch to `Format`, temporarily mapped segment-id mismatch to `DatabaseMismatch`, and temporarily skipped payload CRC verification; targeted corruption tests failed under each mutation and passed after revert. Narrow verification: `cargo test -p strata-storage-next --locked service::wal::tests::corruption` and `cargo test -p strata-storage-next --locked service::wal`. Broad verification: `cargo test -p strata-storage-next --locked`, `cargo test -p strata-storage-next --no-default-features --locked`, `cargo test -p strata-storage-next --all-features --locked`, `cargo clippy -p strata-storage-next --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage-next --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo doc -p strata-storage-next --no-deps --locked`, `cargo fmt --package strata-storage-next --check`, and `git diff --check -- crates/storage-next/src/service/wal.rs crates/storage-next/src/service/wal/tests.rs crates/storage-next/src/service/wal/tests/support.rs crates/storage-next/src/service/wal/tests/corruption.rs docs/architecture/v1-progress-tracker.md docs/architecture/implementation-plans/m3tc2-wal-test-implementation-plan.md docs/architecture/implementation-plans/m3e2-wal-test-suite-plan.md`. |
| `M3TC2E` | Complete | `crates/storage-next/src/service/wal/tests/fault_windows.rs` adds WAL backend fault-window and partial-visibility hardening. Coverage includes active-segment create publish failures, metadata failure before append, list and read failures during replay, delete failure reporting without hiding successful deletes or protected segments, visible partial append failures, visible partial append with misleading metadata, latest partial-tail reopen facts, and non-latest partial-tail corruption. Sensitivity probes: temporarily advanced dirty state before append backend errors, temporarily ignored delete failures, and temporarily treated non-latest partial tails as recoverable; targeted fault-window tests failed under each mutation and passed after revert. Narrow verification: `cargo test -p strata-storage-next --locked service::wal::tests::fault_windows` and `cargo test -p strata-storage-next --locked service::wal`. Broad verification: `cargo test -p strata-storage-next --locked`, `cargo test -p strata-storage-next --no-default-features --locked`, `cargo test -p strata-storage-next --all-features --locked`, `cargo test -p strata-storage-next --features testkit,fault-injection --locked`, `cargo test -p strata-storage-next --no-default-features --features testkit,fault-injection --locked`, `cargo clippy -p strata-storage-next --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage-next --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo doc -p strata-storage-next --no-deps --locked`, `cargo fmt --package strata-storage-next --check`, and `git diff --check -- crates/storage-next/src/service/wal.rs crates/storage-next/src/service/wal/tests.rs crates/storage-next/src/service/wal/tests/support.rs crates/storage-next/src/service/wal/tests/fault_windows.rs docs/architecture/v1-progress-tracker.md docs/architecture/implementation-plans/m3tc2-wal-test-implementation-plan.md docs/architecture/implementation-plans/m3e2-wal-test-suite-plan.md`. |
| `M3TC2F` | Complete | `crates/storage-next/src/service/wal/tests/retention_reopen.rs` adds WAL retention/deletion and deterministic reopen hardening. Retention coverage includes covered old segment deletion, explicit `DeleteObject` capability refusal, active and newer segment protection, above-watermark segment protection, delete failure reporting, non-WAL object exclusion, no-covered-segment reports, and deterministic sorted reports under unsorted backend listings. Reopen coverage includes clean standard close, dirty standard append, always append, rotation, latest partial tail with same-segment and proven would-rotate append refusal, non-latest partial tail strict failure, and corrupt header strict failure. `crates/storage-next/src/service/wal/tests/support.rs` now advertises `DeleteObject` for the stored-byte WAL backend because it implements delete and is the module-local retention backend. Process-kill windows remain deferred to L7/L8. Sensitivity probes: temporarily allowed active segment deletion, temporarily deleted segments when any record was below the retention watermark, and temporarily reopened latest partial tails at the physical object size instead of the valid end offset; targeted retention/reopen tests failed under each mutation and passed after revert. Narrow verification: `cargo test -p strata-storage-next --locked service::wal::tests::retention_reopen`, `cargo test -p strata-storage-next --locked service::wal`, and `cargo test -p strata-storage-next --no-default-features --locked service::wal`. Broad verification: `cargo test -p strata-storage-next --locked`, `cargo test -p strata-storage-next --no-default-features --locked`, `cargo test -p strata-storage-next --all-features --locked`, `cargo test -p strata-storage-next --features testkit,fault-injection --locked`, `cargo test -p strata-storage-next --no-default-features --features testkit,fault-injection --locked`, `cargo clippy -p strata-storage-next --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage-next --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo doc -p strata-storage-next --no-deps --locked`, `cargo fmt --package strata-storage-next --check`, `cargo hack -p strata-storage-next --feature-powerset --depth 2 --locked check --all-targets`, and `git diff --check -- crates/storage-next/src/service/wal.rs crates/storage-next/src/service/wal/tests.rs crates/storage-next/src/service/wal/tests/support.rs crates/storage-next/src/service/wal/tests/retention_reopen.rs docs/architecture/v1-progress-tracker.md docs/architecture/implementation-plans/m3e2-wal-test-suite-plan.md`. |
| `M3TC2G` | Complete | `crates/storage-next/src/service/wal/tests/read.rs` adds explicit read/watermark hardening for zero and max watermarks, duplicate commit versions, out-of-order commit versions, and mixed branch ids. `crates/storage-next/src/service/wal/tests/corruption.rs` adds the shortened-envelope-length mutation with a refreshed envelope CRC. `crates/storage-next/src/service/wal/tests.rs` and `durability.rs` add review-gap assertions for empty existing segment objects, open-time metadata failure routing, and clean close without sync. The append property model now includes larger payloads to exercise rotation-edge paths more often. Sensitivity probes: temporarily changed `read_after_commit_version` from `>` to `>=`, temporarily sorted reads by commit version before filtering, and temporarily made `close` sync unconditionally; targeted tests failed under each mutation and passed after revert. Narrow verification: `cargo test -p strata-storage-next --locked service::wal::tests::read`, `cargo test -p strata-storage-next --locked service::wal::tests::corruption::corrupt_envelope_shortened_length_is_inner_record_format_error`, `cargo test -p strata-storage-next --locked service::wal`, and `PROPTEST_CASES=2048 cargo test -p strata-storage-next --locked wal_append_model`. Broad verification: see final implementation note. |
| `M3TC3A` | Complete | `crates/storage-next/src/service/snapshot/publish_load_tests.rs` adds snapshot construction, publish, load, validation, and capability hardening. Suite cases covered: `ReadObject` capability preflight for load, durable publish/sync capability preflight for snapshot publication, invalid codec id rejection before backend publish, section-kind-zero construction rejection, exact layout object use, publish outcome metadata and durability facts, multi-section roundtrip ordering and opaque payload preservation, backend read failure routing, service-level future/pre-V1/checksum/truncation/invalid-section decode routing, and snapshot load validation through `load_required_for_codec`. `crates/storage-next/src/service/snapshot.rs` now checks `ReadObject` capability before reading a snapshot object. Sensitivity probe: temporarily bypassed the `ReadObject` preflight; `snapshot_load_requires_read_capability_before_backend_access` failed by reaching the backend panic, then passed after revert. Narrow verification: `cargo test -p strata-storage-next --locked snapshot_load_requires_read_capability_before_backend_access`, `cargo test -p strata-storage-next --locked service::snapshot`, and `cargo clippy -p strata-storage-next --all-targets --all-features --locked -- -D warnings`. Broad verification: `cargo test -p strata-storage-next --locked`, `cargo test -p strata-storage-next --no-default-features --locked`, `cargo test -p strata-storage-next --all-features --locked`, `cargo fmt --package strata-storage-next --check`, `cargo doc -p strata-storage-next --no-deps --locked`, `cargo check -p strata-storage-next --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo hack -p strata-storage-next --feature-powerset --depth 2 --locked check --all-targets`, and `git diff --check`. |
| `M3TC3B` | Complete | `crates/storage-next/src/service/snapshot/listing_tests.rs` and `crates/storage-next/src/service/snapshot/listing_property_tests.rs` add snapshot listing, latest, prune, and model hardening. Suite cases covered: empty and ordered listing, exact lowercase snapshot-family parsing, `ListPrefix` capability preflight before parsing, malformed snapshot-family fail-closed behavior, weak-prefix adjacent-family ignores, typed list failures without reads, empty prune reports, retain-count no-op behavior, retain-zero clamping, live MANIFEST snapshot protection, newest-retained protection, sorted deleted/protected/failed reports under unsorted backend listings, per-object delete failure reporting, non-snapshot-family delete exclusion, zero-live rejection before listing, delete-capability preflight before listing, and a host-only `proptest` model over 1 to 96 create/list/latest/prune operations with adjacent-family noise. The property model was later expanded during `M3TC3E` closeout to use real snapshot publish/load service calls and sidecar publish/corrupt/load service calls, closing the full M3TC3 state-machine requirement. Sensitivity probe: temporarily disabled live-snapshot protection during prune; `prune_snapshots_protects_live_snapshot_and_newest_retained` failed by attempting to delete the live snapshot, then passed after revert. Narrow verification: `PROPTEST_CASES=2048 cargo test -p strata-storage-next --locked snapshot_service_state_machine_matches_model`, `cargo test -p strata-storage-next --locked prune_snapshots_protects_live_snapshot_and_newest_retained`, and `cargo test -p strata-storage-next --locked service::snapshot`. Broad verification: `cargo test -p strata-storage-next --locked`, `cargo test -p strata-storage-next --no-default-features --locked`, `cargo test -p strata-storage-next --all-features --locked`, `cargo clippy -p strata-storage-next --all-targets --all-features --locked -- -D warnings`, `cargo doc -p strata-storage-next --no-deps --locked`, `cargo check -p strata-storage-next --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo hack -p strata-storage-next --feature-powerset --depth 2 --locked check --all-targets`, `cargo fmt --package strata-storage-next --check`, `git diff --check`, and engineering scans for forbidden names, roadmap labels, production panics, and file-size thresholds. |
| `M3TC3C` | Complete | `crates/storage-next/src/service/checkpoint/tests/sequencing.rs` adds checkpoint sequencing hardening without growing the existing checkpoint test file beyond the engineering review threshold. Suite cases covered: every active-WAL MANIFEST publish-failure kind stops before snapshot publish, manifest codec/database identity failures leave checkpoint work unstarted, final-MANIFEST uncertainty preserves the exact operation order and leaves the already published snapshot directly loadable, and existing checkpoint tests continue to cover success ordering, invalid input facts before mutation, snapshot publish failures before final facts, orphan-snapshot classification, final-MANIFEST uncertainty classification, and previous MANIFEST fact preservation. Sensitivity probe: temporarily persisted snapshot facts before snapshot publish; `snapshot_publish_failures_do_not_persist_snapshot_manifest_facts` failed with an extra `SnapshotFactsManifest` event before the snapshot publish, then passed after revert. Narrow verification: `cargo test -p strata-storage-next --locked service::checkpoint` and `cargo test -p strata-storage-next --locked snapshot_publish_failures_do_not_persist_snapshot_manifest_facts`. Broad verification: `cargo test -p strata-storage-next --locked`, `cargo test -p strata-storage-next --no-default-features --locked`, `cargo test -p strata-storage-next --all-features --locked`, `cargo clippy -p strata-storage-next --all-targets --all-features --locked -- -D warnings`, `cargo doc -p strata-storage-next --no-deps --locked`, `cargo check -p strata-storage-next --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo fmt --package strata-storage-next --check`, `git diff --check`, and engineering scans for forbidden names, roadmap labels, production panics, and file-size thresholds. |
| `M3TC3D` | Complete | `crates/storage-next/src/service/sidecar/tests/optional_fallback.rs` adds sidecar optional-fallback hardening without expanding the existing sidecar test module further. Suite cases covered: sidecar object construction delegates to the layout namespace, segment id `0` is rejected before backend access, corrupt sidecar fallback preserves the specific decode reason for invalid magic, future version, pre-V1 version, CRC mismatch, and trailing bytes, missing and corrupt sidecars leave authoritative WAL bytes untouched, and both uncertain sidecar publish kinds leave WAL and manifest recovery facts unchanged. `VisibilityUnknown` remains missing in the test harness; `VisibleDurabilityUnconfirmed` leaves the sidecar observable as an optional cache fact. `crates/storage-next/src/service/sidecar/tests.rs` now models `VisibleDurabilityUnconfirmed` by making sidecar bytes visible before returning the publish error, so fault-window tests exercise the post-visibility path. Sensitivity probes: temporarily downgraded sidecar segment-id mismatch from corrupt to missing; `sidecar_segment_id_mismatch_returns_recoverable_corrupt_fact` failed. Temporarily stopped the test backend from making `VisibleDurabilityUnconfirmed` sidecar bytes visible; `sidecar_publish_uncertainty_does_not_mutate_authoritative_recovery_facts` failed. Both probes passed after revert. Broad verification also caught and fixed an object-layout source-guard violation from uppercase reserved layout vocabulary in a checkpoint test comment. Narrow verification: `cargo test -p strata-storage-next --locked service::sidecar`, `cargo test -p strata-storage-next --locked sidecar_segment_id_mismatch_returns_recoverable_corrupt_fact`, and `cargo test -p strata-storage-next --locked sidecar_publish_uncertainty_does_not_mutate_authoritative_recovery_facts`. Broad verification: `cargo test -p strata-storage-next --locked`, `cargo test -p strata-storage-next --no-default-features --locked`, `cargo test -p strata-storage-next --all-features --locked`, `cargo clippy -p strata-storage-next --all-targets --all-features --locked -- -D warnings`, `cargo doc -p strata-storage-next --no-deps --locked`, `cargo check -p strata-storage-next --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo fmt --package strata-storage-next --check`, `git diff --check`, and engineering scans for forbidden names, roadmap labels, production panics, and file-size thresholds. |
| `M3TC3E` | Complete | `crates/storage-next/src/service/snapshot/publish_fault_tests.rs` adds the service-boundary snapshot publish fault-window matrix. Suite cases covered: temporary write and temporary sync failures return `FailedBeforeVisibility` with no visible snapshot object, duplicate immutable snapshot create returns `PreconditionFailed` and preserves prior bytes, backend-reported `VisibilityUnknown` is preserved at the snapshot service boundary, and local filesystem parent-directory sync failure returns `VisibleDurabilityUnconfirmed` while leaving the snapshot directly loadable. Existing checkpoint and sidecar suites cover the remaining M3TC3E matrix rows: snapshot publish failures stop before final MANIFEST snapshot facts, final MANIFEST no-visible failures return orphan snapshot facts, final MANIFEST uncertainty is not reported as an orphan, and sidecar publish uncertainty does not mutate authoritative WAL or snapshot facts. `crates/storage-next/src/backend/local_fs.rs` exposes test-only publish fault injectors for snapshot service tests without adding public testkit API. `crates/storage-next/src/service/snapshot/listing_property_tests.rs` now closes the full M3TC3 service state-machine requirement with generated create snapshot, load snapshot, list, latest, prune, publish sidecar, corrupt sidecar, and load sidecar operations. `crates/storage-next/src/testkit/service_fuzz.rs` and `crates/storage-next/fuzz/fuzz_targets/service_snapshot.rs` pull service fuzzing forward through a hidden bounded operation script over a durable in-memory backend; the existing M3C5 `format_snapshot_envelope` target remains the byte-decoder fuzz target. Sensitivity probe: temporarily collapsed parent-directory sync failure to `FailedBeforeVisibility`; `snapshot_publish_parent_sync_fault_leaves_snapshot_visible_but_unconfirmed` failed, then passed after revert. Narrow verification: `cargo test -p strata-storage-next --locked service::snapshot::publish_fault_tests`, `PROPTEST_CASES=2048 cargo test -p strata-storage-next --locked snapshot_service_state_machine_matches_model`, `cargo test -p strata-storage-next --locked service_fuzz`, `cd crates/storage-next && cargo +nightly fuzz run service_snapshot -- -runs=1024`, `cargo test -p strata-storage-next --locked service::snapshot`, `cargo test -p strata-storage-next --locked service::checkpoint`, and `cargo test -p strata-storage-next --locked service::sidecar`. Broad verification: `cargo test -p strata-storage-next --locked`, `cargo test -p strata-storage-next --no-default-features --locked`, `cargo test -p strata-storage-next --all-features --locked`, `cargo test -p strata-storage-next --features testkit,fault-injection --locked`, `cargo clippy -p strata-storage-next --all-targets --all-features --locked -- -D warnings`, `cargo doc -p strata-storage-next --no-deps --locked`, `cargo check -p strata-storage-next --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo hack -p strata-storage-next --feature-powerset --depth 2 --locked check --all-targets`, `cargo fmt --package strata-storage-next --check`, and `git diff --check`. |
| `M3TE1A` | Complete | `crates/storage-next/src/service/manifest.rs` adds adversarial load/capability hardening for manifest services: non-`NotFound` read failures stay typed as `ManifestServiceError::Read`, codec-validating load preserves backend read failures, memory durable publish failures assert object and source kind, localfs database and table manifest publication assert exact stored bytes, and the publish-fault backend can emit all five `PublishFailureKind` values. Suite cases covered include M3E1 manifest test-suite construction/capability cases, load cases for missing and backend failure, codec-load missing/backend-failure routing, and localfs durable publication byte checks. Sensitivity probe: temporarily routed non-`NotFound` read failures to `Ok(None)`; `database_manifest_load_current_preserves_backend_read_failure` failed, then the mutation was reverted. Narrow verification: `cargo test -p strata-storage-next --locked database_manifest_load_current_preserves_backend_read_failure` and `cargo test -p strata-storage-next --locked service::manifest`. Broad verification: `cargo test -p strata-storage-next --no-default-features --locked service::manifest`, `cargo test -p strata-storage-next --locked`, and `cargo clippy -p strata-storage-next --all-targets --locked -- -D warnings`. |
| `M3TE1B` | Complete | `crates/storage-next/src/service/manifest.rs` adds a durable recording backend for manifest tests, a database manifest model, a host-only `proptest` update-sequence property over 1 to 64 operations, and a raw `publish_current` test proving caller-supplied manifests replace current facts without preservation. The model asserts returned write facts, stored bytes, fresh loads, active WAL segment, snapshot facts, flush watermark, database id, and codec id after every successful update; rejected zero-fact updates assert exact `InvalidRecoveryFact` fields and byte preservation. The `proptest` dev-dependency is gated off wasm so the cache-only wasm compile path remains clean. Sensitivity probe: temporarily changed flush-watermark persistence to drop snapshot facts; `database_manifest_state_machine_preserves_recovery_facts` failed with the minimal sequence snapshot-facts then flush-watermark, then the mutation and probe-only regression seed were removed. Narrow verification: `PROPTEST_CASES=2048 cargo test -p strata-storage-next --locked database_manifest_state_machine_preserves_recovery_facts`, `cargo test -p strata-storage-next --locked service::manifest`, and `cargo test -p strata-storage-next --no-default-features --locked service::manifest`. Broad verification: `cargo test -p strata-storage-next --locked`, `cargo clippy -p strata-storage-next --all-targets --locked -- -D warnings`, `cargo check -p strata-storage-next --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, and `cargo hack -p strata-storage-next --feature-powerset --depth 2 --locked check --all-targets`. |
| `M3TE1C` | Complete | `crates/storage-next/src/service/manifest.rs` adds recovery-fact and codec-validation hardening for manifest services. New tests route invalid codec ids through `ManifestServiceError::Encode` before publish, reject malformed recovery fact bytes as `Decode` with the exact `FormatError::InvalidValue` field, accept valid boundary recovery facts at `1` and `u64::MAX`, prove invalid recovery facts fail before backend access, and distinguish matching codec loads, codec mismatches, corrupt bytes, missing manifests, and backend read failures. Sensitivity probes: temporarily mapped invalid codec errors to `InvalidRecoveryFact`, rejected `u64::MAX` active WAL segments, and mapped corrupt codec-validating loads to `CodecMismatch`; the targeted tests failed under each mutation and passed after revert. Narrow verification: `cargo test -p strata-storage-next --locked service::manifest` and `cargo test -p strata-storage-next --no-default-features --locked service::manifest`. Broad verification: `cargo test -p strata-storage-next --locked`, `cargo clippy -p strata-storage-next --all-targets --locked -- -D warnings`, `cargo check -p strata-storage-next --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, and `cargo hack -p strata-storage-next --feature-powerset --depth 2 --locked check --all-targets`. |
| `M3TE1D` | Complete | `crates/storage-next/src/service/manifest.rs` adds the manifest publish-failure matrix for database/table roles, create/replace intents, and all five `PublishFailureKind` values. The matrix asserts exact `ManifestServiceError::Publish` role, object name, failure kind, and backend source kind; replace-path cases use a stored-byte fault backend to prove prior bytes remain authoritative for no-visible-replacement failures. Sensitivity probes: temporarily collapsed database publish failures to `Unsupported`, wrote replacement bytes before returning `FailedBeforeVisibility`, and mislabeled table publish failures as database errors; the targeted tests failed under each mutation and passed after revert. Narrow verification: `cargo test -p strata-storage-next --locked publish`, `cargo test -p strata-storage-next --locked service::manifest`, and `cargo test -p strata-storage-next --no-default-features --locked service::manifest`. Broad verification: `cargo test -p strata-storage-next --locked`, `cargo clippy -p strata-storage-next --all-targets --locked -- -D warnings`, `cargo check -p strata-storage-next --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, and `cargo hack -p strata-storage-next --feature-powerset --depth 2 --locked check --all-targets`. |
| `M3TE1E` | Complete | `crates/storage-next/src/service/manifest.rs` adds the table-manifest opacity matrix, create/replace opaque-byte semantics, invalid branch-id layout matrix, and role/object/field precision coverage for reachable manifest service error families. Table payload tests cover empty bytes, arbitrary binary bytes, corrupt database-manifest-looking bytes, WAL-looking bytes, embedded NUL bytes, and large payloads without decoding. Layout tests distinguish component-too-long from assembled-object-too-long and prove invalid branch IDs fail before backend access. Sensitivity probes: temporarily decoded table payloads as database manifests, mislabeled table layout errors as database role, and collapsed assembled object-name overflow into component overflow; the targeted tests failed under each mutation and passed after revert. Narrow verification: `cargo test -p strata-storage-next --locked table`, `cargo test -p strata-storage-next --locked service::manifest`, and `cargo test -p strata-storage-next --no-default-features --locked service::manifest`. Broad verification: `cargo test -p strata-storage-next --locked`, `cargo clippy -p strata-storage-next --all-targets --locked -- -D warnings`, `cargo check -p strata-storage-next --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, and `cargo hack -p strata-storage-next --feature-powerset --depth 2 --locked check --all-targets`. |

## Slice Update Protocol

When a slice starts:

1. Add the slice code and title to the owning milestone implementation plan if it
   is not already listed.
2. Add or update a tracker row if the slice changes milestone gate status.
3. Link the slice to any open questions it closes.
4. Link the slice to the test-track work that proves it.

When a slice closes:

1. Record the verification commands in the PR or final implementation note.
2. Mark the slice `Complete` in the issue/PR tracker.
3. Update this file only if the slice changes an epic or milestone status.
4. Do not add completed slice codes to production names or user-facing docs.

## Next Work

The current implementation milestone is `M3`. The next slices are:

1. `M3E4A`: source-map current quarantine behavior and implement the V1
   quarantine inventory codec.
2. `M3E4B-D`: implement inventory service, quarantine/purge mechanics, and
   recovery classifications.
3. `M3TC4`: expand quarantine and recovery-classification tests to
   reference-grade durability coverage.
4. `M3TD1`: prove cache mode creates none of the durable object families after
   quarantine mechanics exist.

`M7` may also begin now if inference-next work should run in parallel with the
storage path.
