---
summary: Delete vectors matching a metadata filter.
mcp_description: Use this when the user wants to remove vectors selected by metadata.
---

Scans the collection for visible vectors matching the metadata filter and deletes the matching rows as a bulk mutation.
