---
summary: Sample vectors with values and version facts.
mcp_description: Use this when the user wants a quick representative look at stored vectors without paging through the whole collection.
---

Returns a deterministic representative sample of vectors from a collection: the total live count and up to `count` entries (default 10), evenly strided over the ordered keys. Latest state only.
