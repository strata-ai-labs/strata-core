---
summary: Sample visible KV rows.
mcp_description: Use this when the user wants a compact sample of KV rows.
---

Returns a deterministic bounded sample of visible KV rows plus the total matching count.
