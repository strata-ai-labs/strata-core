---
summary: Check whether one vector key exists.
mcp_description: Use this when the user only needs to know if a vector key is present.
---

Returns a boolean status for one vector key without loading the embedding.
