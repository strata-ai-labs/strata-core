---
summary: Check whether one JSON document exists.
mcp_description: Use this when the user only needs to know if a JSON document is present.
---

Returns a boolean status for one JSON document key without loading the stored document.
