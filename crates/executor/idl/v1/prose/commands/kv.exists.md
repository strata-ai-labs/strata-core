---
summary: Check whether one KV key exists.
mcp_description: Use this when the user only needs to know if a KV key is present.
---

Returns a boolean status for one KV key without loading the stored value.
