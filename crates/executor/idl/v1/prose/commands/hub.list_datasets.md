---
summary: List datasets from the selected StrataHub.
mcp_description: Use this when a frontend needs to browse hub datasets with StrataHub V1 filters, sorting, and offset pagination.
---

Reads `GET /v1/datasets` from the effective hub URL. Repeatable task, tag, and primitive filters preserve the StrataHub V1 query shape.
