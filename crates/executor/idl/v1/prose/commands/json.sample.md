---
summary: Sample visible JSON documents.
mcp_description: Use this when the user wants a quick look at stored JSON documents without paging through all of them.
---

Returns a bounded sample of visible JSON documents plus the total matching count. Useful for inspecting document shape before writing queries or indexes.
