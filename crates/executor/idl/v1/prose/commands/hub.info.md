---
summary: Read the selected StrataHub's V1 capability advertisement.
mcp_description: Use this when a frontend needs to inspect a hub's protocol version, limits, hash algorithm, or telemetry capability.
---

Reads `GET /v1/info` from the effective hub URL resolved by the shared Strata resolver.
