---
summary: Count visible events in the log.
mcp_description: Use this when the user wants the number of events in the event log, optionally at a historical point in time.
---

Counts events visible in the selected branch and space. The optional timestamp counts the events visible at that commit time.
