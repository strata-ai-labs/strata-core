---
summary: List live refs for a StrataHub dataset.
mcp_description: Use this when a frontend needs to show cloneable branches for a dataset.
---

Reads `GET /v1/datasets/<name>/refs` from the effective hub URL. Yanked refs are filtered by the hub.
