---
summary: List JSON document keys with optional prefix filtering.
mcp_description: Use this when the user wants to page through JSON document keys.
---

Lists visible JSON document keys in byte order. Prefix, cursor, limit, and timestamp parameters constrain the page returned by the executor.
