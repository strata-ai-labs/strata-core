---
summary: Read one StrataHub dataset card.
mcp_description: Use this when a frontend needs the full dataset card before cloning or rendering detail UI.
---

Reads `GET /v1/datasets/<name>` from the effective hub URL and returns the dataset card in an executor-owned JSON envelope.
