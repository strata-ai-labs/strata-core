[package]
name = "strata-gpu-cache"
version = "1.0.0"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish = false
description = "GPU cache tier over the store of record: device runtime + hot-tier machinery"

[dependencies]
# The tier consumes engine-next's public (D4) surface only — a consumer
# like the executor, never a storage-next importer (design §2).
strata-engine-next = { path = "../engine-next" }
tracing = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
