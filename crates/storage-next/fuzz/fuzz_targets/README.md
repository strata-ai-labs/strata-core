# Fuzz Target Stubs

Fuzz targets belong here after the corresponding byte parser or recovery
inventory reader exists. The scaffold intentionally keeps this directory
documentation-only so normal workspace builds do not imply fuzz coverage before
durable bytes exist.
